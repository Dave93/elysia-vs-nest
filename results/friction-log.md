# Friction log

Every surprise while building both apps, timestamped, with the fix. Raw material, no opinions.

## 2026-09-05

- 08:55 — Nest 12 under Bun from TypeScript source (`bun main.ts`): `TypeError: undefined is not an object (evaluating 'descriptor.value')` in `@nestjs/common/decorators/http/request-mapping.decorator.js:12`. Cause: Bun compiles TS with TC39 decorators unless `tsconfig.json` sets `experimentalDecorators: true` and `emitDecoratorMetadata: true`. Fix: add both. Does not affect config B, which runs the SWC-built `dist/`.
- 08:55 — `bun --version` prints `1.4.1`; `bun --revision` prints `1.4.1-canary.1+7b0b70ece`. The revision is what gets recorded.
- 08:57 — `typebox@1.3.26`: neither `Type.Object(...)` nor `Compile(...)` exposes `~standard`. Nest 12's `@Body({ schema })` cannot take TypeBox directly; the sensitivity run uses a hand-written Standard Schema adapter (`apps/nest-api/src/typebox-standard.ts`).
- 08:57 — Nest `StandardSchemaValidationPipe` rejects with 400 and `{"message":[...],"error":"Bad Request","statusCode":400}`; Elysia 2 rejects with 422 and an RFC 9457 body (`type`, `title`, `status`, `detail`, `on`, `property`, `found`, `expected`, `errors`). Accepted difference; the benchmark never sends invalid bodies.
- 08:57 — Nest `@Post` defaults to 201; Elysia to 200. Aligned by `@HttpCode(200)` on `/echo` and `status(201, ...)` on Elysia `/orders`.
- 09:08 — Elysia app: `bun install` for 4 runtime deps + 2 dev deps took 0.83 s wall, 14 packages, `node_modules` 55 MB. `bun build.ts` 0.04 s → `dist/index.js` 790,160 bytes. `bun build-aot.ts` 0.15 s → `dist-aot/index.js` 1,361,590 bytes. The AOT bundle is 72% larger than the plain one: precompiled route handlers and validators are inlined instead of generated at boot. `aot("src/app.ts")` (the instance module) worked first try; `process.exit(0)` after the build was needed as the reference article warned.
- 09:08 — Smoke on `dist`, `dist-aot`, and `dist` with `DB_DRIVER=postgresjs`: all 7 routes + 401 identical. `bun run typecheck` clean.
- 09:09 — `bunx @nestjs/cli@latest new nest-api --package-manager npm --skip-git --strict`: 35.4 s wall including `npm install`. Nest 12 CLI scaffolds an ESM project (`"type": "module"`, `module: nodenext`, `.js` suffixes on relative imports, top-level `await bootstrap()`), TypeScript `^6.0.2`, Vitest 4, oxlint, Prettier, `@nestjs/mau`. `node_modules` 162 MB, 314 packages after scaffold. Express adapter is the default; Fastify is a separate install.
- 09:10 — Adding runtime deps with npm: `npm i @nestjs/platform-fastify@12 drizzle-orm postgres jose zod typebox` 11.8 s; `npm i -D @swc/cli @swc/core` 7.8 s. npm 11.16 prints `npm warn allow-scripts` for packages with install scripts and does not run them until approved.
- 09:10 — `nest build` with `"builder": "swc"` in `nest-cli.json` emitted CommonJS (`Object.defineProperty(exports, "__esModule"...`) into an ESM project. Node 26: `ReferenceError: Cannot determine intended module format because both 'exports' and top-level await are present` (`ERR_AMBIGUOUS_MODULE_SYNTAX`); Bun: `ReferenceError: exports is not defined`. Cause: the CLI's SWC defaults ignore `tsconfig` module settings. Fix: add `.swcrc` with `"module": { "type": "es6" }`, `legacyDecorator` + `decoratorMetadata`. The `tsc` builder would not have needed this.
- 09:11 — TypeBox 1.3 `Validator.Errors()` items carry `instancePath`/`schemaPath`, not `path`. Adapter fixed accordingly.
- 09:11 — Nest build with SWC: 0.67 s wall, `dist/` 40 KB (unbundled, 10 files). Smoke on Node, on Bun, and with `VALIDATOR=typebox` on Node: all 7 routes + 401 identical to Elysia. `tsc --noEmit` 1.1 s (TypeScript 6.0.2).
- 09:25 — Full run, config A `/health` at c=500: bombardier reported 1,129 non-2xx in run 1 and 11,183 in run 2 (≈0.04 % and ≈0.4 % of requests). c=10 and c=100 had zero. `sysctl kern.ipc.somaxconn` = 128 on this Mac, so 500 concurrent connections can overflow the listen backlog whenever connections churn. To attribute (Node-only vs host-wide) compare B/C/D at c=500 in `results.json`, then run a 10 s c=500 diagnostic that keeps bombardier's `req4xx/req5xx/others` split. The harness stores only the sum.
- 09:55 — Config A finished. `/cpu?n=15000` at c=100: median 31,180 rps with 910,605 non-2xx; at c=500: 39,150 rps with 1,152,875 non-2xx. At c=10 the same route served 192 rps with zero errors and p99 95 ms. A 15,000-hash loop cannot complete 31k times per second, so most of those responses are fast non-2xx from the server while the event loop is pinned. Diagnostic queued after the full run: 10 s at c=100 with bombardier's `req4xx/req5xx/others` split and a sampled error body. Compare with B (same dist on Bun) and C/D.
- 09:55 — Config A boot-to-healthy during the run: 972 ms (single cold sample, first process after seeding). `boot.ts` will give the 10-run median.
- 10:29 — Config B (same Nest `dist/` on Bun) finished. At c=500 non-2xx fell from 502–30,748 per run on Node to 0 on five routes, 101 on `/me`, 12 on `/users` list. So the c=500 error rate on A is Node's HTTP server under this load, not only the 128 listen backlog. `/cpu` at c=100 on Bun: 180 rps, p99 619 ms, zero errors, versus Node's 910,605 errors on the same combination. At c=500 both runtimes show the same fast-error mode (B: 1,136,021 non-2xx), consistent with bombardier's 2 s per-request timeout once 500 × ~5.5 ms of queue exceeds it, then reconnects hitting a full backlog.
- 10:29 — B `/orders` (insert): 18,670 rps at c=10 but 13,110 rps at c=100 with mean CPU 74 % and p99 134 ms at c=500. CPU idle while throughput falls → waiting on the 10-connection pool or Postgres. Compare with B-pgjs (postgres.js on Bun) in the sensitivity run and with C/D.
- 10:29 — Idle RSS during the run: A 196 MB, B 91 MB. Boot during the run: A 972 ms, B 425 ms (single samples; `boot.ts` gives medians).
- 11:50 — `bench/diag.ts` (10 s probes, bombardier `-l --format json`, full error split). Config A `/health` c=500: 629,904 2xx, 0 4xx, 0 5xx, 6,751 `others` — "dial tcp 127.0.0.1:3000: connect: connection reset by peer" and "the server closed connection before returning the first response byte". Config A `/cpu` c=100: 1,633 2xx and 165,298 `others`, 153,710 of them connection resets: while the loop is pinned, Node's HTTP server resets incoming connections and bombardier reconnects in a tight loop, which is where the "31k rps with 900k errors" median came from. Config C (Elysia/Bun) same probes: `/health` c=500 1,274,191 2xx and 0 errors; `/cpu` c=100 2,171 2xx, 0 errors, p99 506 ms. Every A non-2xx in `results.json` is a connection-level error, never an HTTP error status.
- 11:50 — Two earlier `diag.ts` runs died silently on `raw.latency.percentiles` being undefined without bombardier's `-l` flag; the crash text was lost inside a backgrounded pipeline. Fixed by adding `-l`.
- 11:40 — Bun upgraded: `bun --revision` now `1.4.2+744846f84` (release notes 2026-09-05: AsyncLocalStorage leak, worker_threads `online` order, musl GC crash; nothing in HTTP, SQL, crypto or bundler). Elysia bundles rebuilt on 1.4.2 (`dist/index.js` 790,226 B, `dist-aot/index.js` 1,361,656 B). Parity re-passed across all six configs. B/C/D re-measured on 1.4.2; the 1.4.1-canary run is kept as `results/results-bun141canary.json`.
- 12:23 — Bun 1.4.2 rerun of B came in 6–19 % below the canary run at c=100, including `/cpu` (pure crypto). Cause found on the host, not in Bun: another session's `tsc --noEmit` (from `~/development/miss/apps/admin`) at 104 % CPU during the run, on top of a steady baseline of iMe.app 87–91 %, Raycast 60–85 % and WindowServer 65–78 % that has been present for 14 h. Load average 19 on 14 cores. That partial pass is kept as `results/results-bun142-contended-partial.json` and not used.
- 12:25 — Harness now samples foreign CPU (every process except the server, bombardier and itself) once per second during every run, stores `otherCpu` per repeat, waits at start until foreign CPU is below `CONTENTION` (400 % here, above the ~250–300 % steady baseline), and redoes any 30 s repeat that saw foreign CPU at or above it (max 3 retries per combination). B/C/D relaunched on 1.4.2 with that gate. The morning A/B/C/D run predates the sampler, so its foreign load is unknown beyond the baseline apps; `drift.ts` re-measures A afterwards to bound the difference.
